# 安全政策

## 报告安全问题

远程仓库创建后将启用 GitHub Private Vulnerability Reporting。可能导致数据泄露、备份绕过、任意文件访问、签名伪造或通知权限滥用的问题，请通过仓库 Security 页面私下报告，不要创建公开 Issue。

报告建议包含：受影响版本、平台、复现条件、预期影响和可公开时间。请勿附带真实用户数据库、真实备份或密钥；使用最小化的合成测试数据。

## 当前安全边界

- 应用不依赖账号或业务服务器。
- Android 正式版本不声明 `INTERNET` 权限。
- 数据保存在应用沙箱和用户主动选择的本地备份位置。
- 备份加密、数据库迁移、签名构建和 Release 校验属于安全关键路径，必须经过独立测试。
