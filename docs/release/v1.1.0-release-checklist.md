# 当归 v1.1.0 发布检查表

- 状态：**待标签 CI 生成公开预发布（Pre-release；非稳定版）**
- 产品版本：`1.1.0`
- 技术版本：`1.1.0+2`
- Android versionCode：通用/AAB `2`；`armeabi-v7a` `1002`；`arm64-v8a` `2002`；`x86_64` `4002`
- 目标公开 Pre-release：[v1.1.0](https://github.com/hujizhou35-cmd/danggui-app/releases/tag/v1.1.0)

本页是 v1.1.0 的发布事实源。只有标签工作流和实体机证据真实产生后才能勾选对应项目。`debug-fallback`、CI 临时包、unsigned `.app.zip` 均不是可对外传播的正式安装包；代表性 Android 实体机验收签署前不得将 v1.1.0 改称稳定版。

## 1. 三项重点修复

- [x] 事项、笔记、过往共用稳定的编辑布局；真实路由进入后可输入，键盘收起后顶栏、正文与工具栏正确复位，返回前自动保存已 flush。
- [x] Flutter 启动页恢复无文字水彩球与嫩芽插画，并动态叠加“当归”、红印章、隐私文案、真实加载环和加载文字；水彩插画完成解码并进入可见帧后计满一秒，且初始化成功时只导航一次；资源异常设有有界兜底，不会永久卡住。
- [x] 所有新建事项入口默认本地当天，同时允许清除日期；事项详情可创建、修改、删除提醒并呈现权限受限、关闭暂停和已过期状态。
- [ ] 使用用户提供的正式 v1.0.0 APK 在原手机完成覆盖升级，确认事项、提醒、笔记、过往和设置全部保留并复验以上三项。

## 2. 自动化与 Android 产物

- [x] 版本、备份、导出、平台审计和构建校验从 `pubspec.yaml`/共享运行时常量取得一致版本。
- [ ] `flutter analyze --fatal-infos` 与完整 Flutter 测试通过。
- [ ] Android API 24/36 标签 CI 的 **Debug 插桩交互阶段** 完成真实键盘编辑、同签名覆盖、权限、AlarmManager、真实系统通知出现及通知内容点击回前台验收；通知出现后，生产回调处理器须依次用真实原生网关和数据库证明 10/30/60 分钟重排。此阶段不是公开 Release APK 的运行证明，也不声称自动点击三种稍后提醒按钮。
- [ ] 同一 API 24/36 作业下载 `android-linux` 的同一 SHA 通用 **Release-mode APK**，复核 SHA-256 后卸载测试包并原样安装；纯 ADB/UIAutomator 冷启动、包名/`1.1.0`/versionCode `2`、事项→笔记→设置→事项导航、稳定 PID 与无崩溃扫描全部通过。标签 run 还必须记录 `SIGNING_MODE.txt=release`；普通分支的 `debug-fallback` 只能证明 Release 构建模式，不能称为正式包。
- [ ] 标签产出的 `SIGNING_MODE.txt` 为 `release`，通用 APK、三个 ABI APK 与 AAB 的 ID、版本、SDK、权限及签名全部通过最终产物校验。
- [ ] `SIGNING_CERTIFICATE_SHA256.txt` 与维护者保存的正式 v1.0.0 证书指纹一致，`SHA256SUMS` 覆盖除清单自身以外的全部公开附件。

## 3. iOS 源码与构建证据

- [x] `tool/build_ios_source_zip.sh` 从干净标签提交确定性生成 `danggui-ios-source-v1.1.0.zip`，且排除密钥、描述文件、缓存和 `dist`。
- [x] 源码 ZIP 包含完整已跟踪 Flutter/iOS 源码、Xcode 工程、锁定依赖、品牌/帮助资源、四语本地化、许可证、`test/`、`integration_test/` 和构建说明。
- [ ] macOS 标签 CI 的 unsigned `Runner.app` 构建、Bundle ID、`1.1.0+2`、iOS 15、竖屏、后台模式及远程通知 entitlement 审计通过。
- [x] 文档与 Release 说明明确：unsigned `.app.zip` 不能直接安装且不是 IPA；v1.1.0 不提供 IPA/TestFlight，也不声称通过 App Store 审核。

## 4. 发布与稳定版门禁

- [x] `v*` 标签工作流在 Android、API24、API36、iOS 四项保护作业全绿后，才组装并创建/刷新 GitHub Pre-release。
- [ ] 标签 `v1.1.0` 指向合并后的 `main`，所有必需检查通过，公开附件可从 Release 下载并复算哈希。
- [x] 四语 README、Changelog、平台交付、隐私审计和 iOS 源码构建说明已更新到 v1.1.0；v1.0.0 历史 Changelog 与检查表保留。
- [ ] 代表性 Android 实体机完成 OEM 后台、锁屏、声音、振动与稍后提醒验收并签署；签署前保持 Pre-release。
