# 当归 v1.0.0 发布检查表

- 状态：**首发准备（release candidate）**
- 版本：`1.0.0+1`
- 正式 Release URL：**尚未创建；门禁通过后填写**

本页是 v1.0.0 对外发布状态的事实源。“待确认”不是失败，也不代表已经通过；只有对应证据产生后才能勾选。CI Artifact 不等于 GitHub Release，文件名含 `debug-fallback` 的包永远不能作为正式包发布。

## 1. 源码与质量门禁

- [ ] `flutter analyze --fatal-infos` 无错误、警告或 info。
- [ ] 单元、数据库、Widget、Golden、备份恢复、提醒和压力测试全部通过。
- [ ] Android 与 iOS 的离线边界静态审计通过。
- [ ] Golden 图逐张人工检查，无溢出、乱码、截断或品牌错图。
- [ ] GitHub Actions 的 `android-emulator-smoke` 在 API 24/36 x86_64 真实模拟器上通过冷启动、四主导航与离线帮助可达性验证，且无未处理 Flutter 异常。
- [ ] API 24 与 API 36 完成全新安装、覆盖升级、提醒及数据保留验证。

## 2. Android 正式产物

- [ ] 版本标签构建的 `SIGNING_MODE.txt` 为 `release`。
- [ ] 通用 APK 与三个 ABI 分包均存在；AAB 存在。
- [ ] `apksigner` 与 AAB JAR 签名验证通过。
- [ ] 证书 SHA-256 与维护者保存的预期指纹一致，并在 Release 说明公开。
- [ ] 最终 APK 的 application ID、版本、SDK、`debuggable` 和权限集合通过审计。
- [ ] `SHA256SUMS` 覆盖所有公开 APK/AAB，并在下载后复核。

正式附件合同：

- `danggui-android-universal-release.apk`
- `danggui-android-armeabi-v7a-release.apk`
- `danggui-android-arm64-v8a-release.apk`
- `danggui-android-x86_64-release.apk`
- `danggui-android-release.aab`
- `SHA256SUMS`

## 3. iOS 交付边界

- [ ] Xcode 工程与完整源码已进入 v1.0.0 提交。
- [ ] macOS CI 的 unsigned 构建与最终 `.app` 配置审计通过。
- [ ] `danggui-ios-unsigned.app.zip` 只标注为构建证据，并明确“不可安装、不是 IPA”。
- [ ] Release 说明明确：v1.0.0 不提供 IPA，不进入 TestFlight，也不声称 App Store 审核通过。

## 4. 隐私、数据与文档

- [ ] 最终 Android APK 不含 `INTERNET`、精确闹钟或全屏通知权限。
- [ ] iOS 未加入远程推送、CloudKit 或后台网络 entitlement/capability。
- [ ] 四语离线帮助覆盖事项、提醒、过往、笔记、备份、恢复、导出与最近删除操作。
- [ ] README、Changelog、Release 说明、`NOTICE`、第三方许可与品牌边界一致。
- [ ] 加密备份密码不可恢复、iOS unsigned、提醒可能延迟等限制已公开。

## 5. 发布与传播

- [ ] GitHub Actions 的版本标签工作流全部成功。
- [ ] Release 页面创建后再把本页 URL 字段替换为真实地址。
- [ ] Release 附件、校验和和公开证书指纹从下载端再次验证。
- [ ] GitHub Social Preview 使用 `docs/assets/brand/danggui-social-preview.jpg`。
- [ ] 发布后 README 状态从“首发准备”改为“v1.0.0 已发布”；四种语言同步。
- [ ] 只有上述步骤完成后才生成指向 `/releases/latest` 的二维码或“立即下载”素材。
