# 当归 v1.0.0 发布检查表

- 状态：**公开预发布（Pre-release；非稳定版）**
- 版本：`1.0.0+1`
- 标签提交：`b6dc50594abdb769080a738dd64550d26bc64d36`
- 标签 CI：[workflow run 32609272408](https://github.com/hujizhou35-cmd/danggui-app/actions/runs/32609272408)（四个作业全部成功）
- 公开 Pre-release：[v1.0.0](https://github.com/hujizhou35-cmd/danggui-app/releases/tag/v1.0.0)

本页是 v1.0.0 对外发布状态的事实源。“待确认”不是失败，也不代表已经通过；只有对应证据产生后才能勾选。CI Artifact 不等于 GitHub Release，文件名含 `debug-fallback` 的包永远不能作为正式包发布。当前页面只能传播为 **Pre-release**；代表性 Android 实体机验收签署前不得改称稳定版。

## 1. 源码与质量门禁

- [x] `flutter analyze --fatal-infos` 无错误、警告或 info。
- [x] 单元、数据库、Widget、Golden、备份恢复、提醒和压力测试全部通过。
- [x] Android 与 iOS 的离线边界静态审计通过。
- [x] Windows/Linux Golden 图逐张人工检查，无溢出、乱码、截断或品牌错图。
- [x] GitHub Actions 的 `android-emulator-smoke` 在 API 24/36 x86_64 真实模拟器上通过冷启动、四主导航与离线帮助可达性验证，且无未处理 Flutter 异常。
- [x] API 24 与 API 36 模拟器完成全新安装、同版本同签名覆盖以及 37/37 项验收：事项、提醒、笔记、文件夹、过往、设置六域保留，SQLite 完整性、卡片提醒文案、AlarmManager 与真实系统通知均通过；API 36 还观察并操作了真实系统通知权限弹窗。
- [ ] 代表性 Android 实体机完成正式 APK 覆盖、提醒可见性、锁屏、声音、振动及 10/30/60 分钟稍后提醒人工验收，并签署 `docs/qa/release-device-acceptance.md`。
- [ ] 使用真实旧 schema 数据完成迁移验收并保留证据（本次同版本覆盖自动化不证明旧 schema 迁移）。

## 2. Android 正式产物

- [x] 版本标签构建的 `SIGNING_MODE.txt` 为 `release`。
- [x] 通用 APK 与三个 ABI 分包均存在；AAB 存在。
- [x] `apksigner` 与 AAB JAR 签名验证通过。
- [x] 证书 SHA-256 与维护者保存的预期指纹一致，并在 Release 说明公开。
- [x] 最终 APK 的 application ID、版本、SDK、`debuggable` 和权限集合通过审计。
- [x] `SHA256SUMS` 覆盖所有公开 APK/AAB；公开 Pre-release 的 16 个附件均已从下载端取回并复算 SHA-256。

正式附件合同：

- `danggui-android-universal-release.apk`
- `danggui-android-armeabi-v7a-release.apk`
- `danggui-android-arm64-v8a-release.apk`
- `danggui-android-x86_64-release.apk`
- `danggui-android-release.aab`
- `SHA256SUMS`

## 3. iOS 交付边界

- [x] Xcode 工程与完整源码已进入 v1.0.0 提交。
- [x] macOS CI 的 unsigned 构建与最终 `.app` 配置审计通过。
- [x] `danggui-ios-unsigned.app.zip` 只标注为构建证据，并明确“不可安装、不是 IPA”。
- [x] Release 说明明确：v1.0.0 不提供 IPA，不进入 TestFlight，也不声称 App Store 审核通过。

## 4. 隐私、数据与文档

- [x] 最终 Android APK 不含 `INTERNET`、精确闹钟或全屏通知权限。
- [x] iOS 未加入远程推送、CloudKit 或后台网络 entitlement/capability。
- [x] 四语离线帮助覆盖事项、提醒、过往、笔记、备份、恢复、导出与最近删除操作。
- [x] README、Changelog、Release 说明、`NOTICE`、第三方许可与品牌边界一致。
- [x] 加密备份密码不可恢复、iOS unsigned、提醒可能延迟等限制已公开。

## 5. 发布与传播

- [x] GitHub Actions 的版本标签工作流全部成功（[run 32609272408](https://github.com/hujizhou35-cmd/danggui-app/actions/runs/32609272408)，四个作业全绿）。
- [x] 公开 Pre-release 已创建，本页已记录其真实 URL。
- [x] Release 的 16 个附件、校验和与公开证书指纹已从下载端再次验证。
- [ ] GitHub Social Preview 使用 `docs/assets/brand/danggui-social-preview.jpg`。
- [x] README 四语状态均明确为“v1.0.0 公开预发布”，并指向真实标签 Release URL。
- [ ] 仅在实体机清单签署并转为稳定版后，才生成指向稳定 Release 的二维码或“立即下载”素材。
