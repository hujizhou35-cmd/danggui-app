# 当归 v1.1.5 发布检查表

- 产品版本：`1.1.5`
- 技术版本：`1.1.5+6`
- Android versionCode：通用/AAB `6`；`armeabi-v7a` `1006`；`arm64-v8a` `2006`；`x86_64` `4006`
- 审计基线：`v1.1.4` / `2e6e67a2`
- 目标公开 Pre-release：[v1.1.5](https://github.com/hujizhou35-cmd/danggui-app/releases/tag/v1.1.5)

本页是 v1.1.5 的发布事实源。只有代码、门禁、标签工作流和附件实际产生后才能勾选。`contract-proven` 只表示合同和故障路径自动化通过；`simulator-proven` 只表示指定模拟器运行时通过；两者均不等于 iPhone 实体机验收。

## 可追溯审计

- [x] `docs/research/ios-v1.1.5/` 固定 v1.1.4 基线、F01–F28 行为合同、54 个来源、许可证、评分、反例、矩阵、风险缺口和设计决策。
- [x] 14 个 P0 均包含 Apple 规则、两个成熟实现（至少一个原生 iOS）及相关失败案例。
- [ ] 所有实际修复均能回指失败测试和设计决策；无证据的现有实现不重写。
- [ ] 依赖、引用代码和发行附件通过许可证及离线隐私审计。

## P0 提醒与平台链

- [ ] 原生镜像/事务/取消标记遇到部分损坏时优先完整 LKG，不让不完整主文件污染备份。
- [ ] AlarmKit 缺失恢复只在观察到 alerting 或权威动作后记录 delivered/systemAlert；15 分钟内至多执行一次保守补登记。
- [ ] AlarmKit 和普通通知的 Stop/Snooze 均绑定 reminder/revision/session，旧操作与重复操作幂等。
- [ ] 首次排期、稍后提醒和原生事件都保留 canonical IANA zone，并有 DST 回归。
- [ ] 15 分钟过期、60/64 容量、权限撤销/恢复、冷启动、替换、取消、快照和诊断合同通过。

## P0 数据、备份与恢复链

- [ ] SQLite 启动执行完整 `integrity_check`/`foreign_key_check`，WAL、迁移、损坏恢复和未来 schema 拒绝均有测试。
- [ ] `.dgbak` 的一致性快照、哈希、加密认证、原子交换、安全副本、merge 幂等和逐阶段故障注入通过。
- [ ] 数据库、自动备份、原生提醒镜像和诊断明确排除 iCloud 备份，并采用受保护文件策略。
- [ ] Keychain 使用本机不可同步可访问性；临时明文、口令、路径、正文和密钥不进入日志或残留目录。
- [ ] 恢复后来源设备 platform ID 不复用，旧系统提醒退休，未来提醒由新设备 outbox 重建。

## P1/P2 与跨模块

- [ ] 全量现有 Flutter 测试、金图、四语、编辑器、搜索、导出、最近删除、设置和无障碍回归通过。
- [ ] 固定 seed 的 640 步事项/笔记模型测试通过，数据库完整 `integrity_check`/`foreign_key_check` 保持正常。
- [ ] 两条 XCUITest 覆盖“事项—提醒—删除恢复”和“备份—恢复—提醒重建”。
- [ ] iPhone/iPad、小屏、Dynamic Type、四语、长文档与大数据量深入门禁通过或明确登记证据缺口。
- [ ] Android 静态、原生、API 24/36 模拟器、APK/AAB、签名、权限、架构、版本与 SHA-256 回归通过。

## 固定免费 CI

- [ ] 标准 `macos-15`、Xcode 16.4、iOS 18.5 Simulator 验证 iOS 15–25 回退路径。
- [ ] 标准 `macos-26`、Xcode 26.6、iOS 26.5 Simulator 验证 AlarmKit 路径。
- [ ] Flutter 3.47.1、Dart 3.13.1、Xcode、依赖锁及 sqlite3 原生文件 SHA-256 固定。
- [ ] 两条 iOS 作业均通过 RunnerTests、RunnerUITests、unsigned device build；失败上传 `.xcresult`，成功只保留摘要。
- [ ] 未使用 `macos-latest`、large runner、付费服务、Apple 会员、ToDesk 或云 Mac。

## 合并、标签与附件

- [ ] `pubspec.yaml`、运行时常量、设置、导出、README 和审计测试统一为 `1.1.5+6`。
- [ ] PR 通过保护分支检查并合入 `main`；不可变 `v1.1.5` 标签精确指向合入提交。
- [ ] 顶层附件精确为 `danggui-android-universal-release.apk`、`danggui-ios-source-v1.1.5.zip`、`danggui-developer-assets-v1.1.5.zip` 与 `SHA256SUMS`。
- [ ] 正式 APK 使用受保护签名且证书 SHA-256 与仓库环境一致；四个附件校验值发布后复核。
- [ ] Release 保持 Pre-release，不提供 IPA/TestFlight，不改写 v1.1.4 及更早标签、附件或校验值。

## 明确未验证

- [ ] Release 清楚列出真实 iPhone 声音、触感、静音/专注、隔夜锁屏、重启及进程被系统终止后的投递为 `device-unverified`。
- [ ] iOS 15–17 仅有 deployment-target、可用性分支及合同证据，未宣称对应运行时已验证。
- [ ] 小米及其他 OEM Android 若无本轮 20 次实体锁屏测试，作为不阻塞 Pre-release 的未验证项列出。
